<h1>Welcome new user!</h1>
	<p>
	<strong>Title:</strong>
	<ul>
	<% @posts.each do |post| %>
		<li><%= post.title %></li>
		<li><%= post.content %></li>
	<% end %>
	</ul>

  	<%= link_to "create new", new_user_post_path(@user, @post) %>
	</p>
